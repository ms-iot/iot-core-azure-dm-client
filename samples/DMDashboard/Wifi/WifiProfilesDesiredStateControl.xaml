<UserControl x:Class="DMDashboard.Wifi.WifiProfilesDesiredStateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DMDashboard.Wifi"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions> <RowDefinition Height="Auto" /> </Grid.RowDefinitions>
        <Grid.ColumnDefinitions> <ColumnDefinition Width="Auto" /> </Grid.ColumnDefinitions>
        <ListView Grid.Row="0" Grid.Column="0" x:Name="desiredList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:WifiDesiredStateControl/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
    <!--
    <StackPanel Orientation="Vertical">
        <local:WifiDesiredStateControl x:Name="App0"/>
        <local:WifiDesiredStateControl x:Name="App1"/>
        <local:WifiDesiredStateControl x:Name="App2"/>
        <local:WifiDesiredStateControl x:Name="App3"/>
    </StackPanel>
    -->
</UserControl>
